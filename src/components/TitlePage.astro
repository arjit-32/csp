---
import Title from './Title.astro'
import Shape from './icons/Shape.astro'
type Props = {
	title: string
}

const { title } = Astro.props
---

<div class='flex flex-col text-white justify-center items-center gap-2 title min-h-[240px] my-10 p-5 bg-gradient-to-r from-indigo-500 from-10% via-sky-500 via-30% to-emerald-500 to-90%'>
	<!-- <Shape /> -->
	<Title>{title}</Title>
	<p class="font-thin mt-5">
		<slot />
	</p>
</div>
